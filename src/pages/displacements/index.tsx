import { PageContainer } from '@/src/css/global';
import { FC } from 'react';

const Displacements: FC = () => {
  return <PageContainer> Deslocamentos</PageContainer>;
};

export default Displacements;
